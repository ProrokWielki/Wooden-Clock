syntax = "proto3";

import "nanopb.proto";

message Time {
  uint32 hours   = 1 [(nanopb).int_size = IS_8] ;
  uint32 minutes = 2 [(nanopb).int_size = IS_8] ;
  uint32 seconds = 3 [(nanopb).int_size = IS_8] ;
}