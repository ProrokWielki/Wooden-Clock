syntax = "proto3";

import "nanopb.proto";

enum Flash {
  INTERNAL = 0;
  EXTERNAL = 1;
}

message FlashingStart {
  Flash  flash        = 1;
  uint32 start_page   = 2 [(nanopb).int_size = IS_8] ;
  uint32 num_of_pages = 3 [(nanopb).int_size = IS_8] ;
}
