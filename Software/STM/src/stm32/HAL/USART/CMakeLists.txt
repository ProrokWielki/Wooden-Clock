ADD_LIBRARY(
  USART STATIC EXCLUDE_FROM_ALL
  include/HAL/USART.hpp USART.cpp)

TARGET_INCLUDE_DIRECTORIES(USART PUBLIC include)

TARGET_LINK_LIBRARIES(
  USART
  PRIVATE CmsisBridge HalTypes Clock
  PUBLIC Register GPIO)
