ADD_LIBRARY(
  SPI STATIC EXCLUDE_FROM_ALL
  include/HAL/SPI.hpp SPI.cpp)

TARGET_LINK_LIBRARIES(
  SPI
  PUBLIC Register
  PRIVATE CmsisBridge HalTypes Clock GPIO)

TARGET_INCLUDE_DIRECTORIES(SPI PUBLIC include)
