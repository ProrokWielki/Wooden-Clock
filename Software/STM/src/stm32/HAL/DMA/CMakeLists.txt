ADD_LIBRARY(
  DMA STATIC EXCLUDE_FROM_ALL
  DMA.cpp include/HAL/DMA.hpp)

TARGET_LINK_LIBRARIES(
  DMA
  PRIVATE CMSIS HalTypes APP CmsisBridge Clock Register)

TARGET_INCLUDE_DIRECTORIES(DMA PUBLIC include)
