ADD_LIBRARY(
  QSPI STATIC EXCLUDE_FROM_ALL
  include/HAL/QSPI.hpp QSPI.cpp)

TARGET_LINK_LIBRARIES(
  QSPI
  PRIVATE CmsisBridge HalTypes Clock GPIO Register)

TARGET_INCLUDE_DIRECTORIES(QSPI PUBLIC include)
