ADD_LIBRARY(
  RTC STATIC EXCLUDE_FROM_ALL
  include/HAL/RTC.hpp RTC.cpp)

TARGET_LINK_LIBRARIES(
  RTC
  PRIVATE Register HalTypes CmsisBridge Clock Power)

TARGET_INCLUDE_DIRECTORIES(RTC PUBLIC include)
