ADD_LIBRARY(
  GPIO STATIC EXCLUDE_FROM_ALL
  GPIO.cpp include/HAL/GPIO.hpp)

TARGET_LINK_LIBRARIES(
  GPIO
  PRIVATE CmsisBridge Clock
  PUBLIC Register HalTypes)

TARGET_INCLUDE_DIRECTORIES(GPIO PUBLIC include)
